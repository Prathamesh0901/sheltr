{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["DATABASE_URL"], 
  "build": {

  },
  "tasks": {
    "dev": {
      "persistent": true,
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}